---
title: "`dead-oob`"
description: "Expanded narrative for the dead-oob rule: the failure it catches, why nothing else sees it, and the fix the stack expects."
---

:::note[TODO]
Expanded narrative for the dead-oob rule: the failure it catches, why nothing else sees it, and the fix the stack expects. Fill this page on the next pass — prefer expanding this stub over adding a new sidebar section.
:::
